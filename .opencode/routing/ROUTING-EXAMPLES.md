# Examples

### Simple
Request: "Find where checkout validation is implemented."

Route:
cheap-worker -> combo-free

### QA
Request: "Create test coverage for checkout API."

Route:
planner -> qa-lead -> test-designer -> api-tester
Gemini tier.

### Normal feature
Request: "Add a filter to the order list."

Route:
planner -> developer-standard -> test-designer -> relevant tester -> code-reviewer
Gemini/Sonnet/Opus.

### Complex feature
Request: "Redesign checkout tax calculation."

Route:
planner -> architect -> debater -> developer -> QA -> tests -> code-reviewer -> release-gate
Gemini/Opus.

### Security
Request: "Add OAuth callback handling."

Route:
architect -> developer -> security-reviewer -> code-reviewer -> QA -> release-gate
Opus for critical reasoning.
